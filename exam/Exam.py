import pandas as pd
import numpy as np

class student (Exception)
    def __init__  (self name, scores)
 self.Name = Name
 self.Scores = scores
    

    score = (75, 85,)
def __str__(self):
        return f"{self.name} ({self.scores}